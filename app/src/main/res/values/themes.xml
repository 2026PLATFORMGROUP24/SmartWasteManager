<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Must extend a Theme.MaterialComponents (AppCompat-based) rather than
        android:Theme.Material, because compose-markdown's CustomTextView (via Markwon)
        is an AppCompat widget and throws ThemeUtils errors with a plain Material theme.
    -->
    <style name="Theme.SmartWasteManager" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>